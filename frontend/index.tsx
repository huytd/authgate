import * as React from 'react'
import { render } from 'react-dom'

const AdminDashboard = () => {
    return (
        <div>
            Hello World
        </div>
    )
}

render(<AdminDashboard />, document.getElementById('root'))